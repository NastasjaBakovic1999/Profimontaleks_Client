import { Phase } from "./phase";
import { PhaseStatus } from "./phaseStatus";

export class ProductCardboardPhase {
    pccNumber: number;
    phaseId: number;
    statusId: number;
    phase: Phase;
    status: PhaseStatus;
}